// Reverse an array using spread operator

const names = ['alen', ' walker', 'charlie']

const reversed = [...names].reverse()

console.log(names) // ['alen', ' walker', 'charlie']

console.log(reversed) // ['charlie', ' walker', 'alen']
