// Implements the sum function
function sum(x, y) {
  return x + y
}

// Implements the subtract function
function substract(x, y) {
  return x - y
}

// Implements the multiply function
function multiply(x, y) {
  return x * y
}

// Implements the divide function
function divide(x, y) {
  return x / y
}
