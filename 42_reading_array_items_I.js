let grades = [5, 3, 8]
grades[1] // 3

// //===========================================================================================================================

let grades1 = [10, 5, 13]
let firstGrade = grades1[0]
console.log(firstGrade)

// //===========================================================================================================================
function getFullName(names) {
  return `${names[0]} ${names[1]}`
}

// //===========================================================================================================================
function describeShopLocation(shop, coordinates) {
  return `The ${shop} is located at ${coordinates[0]}, ${coordinates[1]}`
}

function calculateDifference(firstRactangle, secondRectangle) {
  return (
    firstRectagnle[0] * firstRactangle[1] -
    secondRectangle[0] * seoncdRectangle[1]
  )
}
